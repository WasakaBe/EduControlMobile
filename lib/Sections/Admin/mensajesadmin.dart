import 'package:flutter/material.dart';

class MensajesAdminScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Mensajes Administrativos', style: TextStyle(fontSize: 24)),
    );
  }
}

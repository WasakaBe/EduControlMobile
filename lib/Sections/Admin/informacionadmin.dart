import 'package:flutter/material.dart';

class InformacionAdminScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Información Administrativa', style: TextStyle(fontSize: 24)),
    );
  }
}

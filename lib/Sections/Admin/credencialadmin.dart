import 'package:flutter/material.dart';

class CredencialAdminScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Credencial del Administrador', style: TextStyle(fontSize: 24)),
    );
  }
}
 